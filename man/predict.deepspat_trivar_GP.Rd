% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.deepspat_trivar_GP.R
\name{predict.deepspat_trivar_GP}
\alias{predict.deepspat_trivar_GP}
\title{Deep trivariate compositional spatial model}
\usage{
\method{predict}{deepspat_trivar_GP}(object, newdata, ...)
}
\arguments{
\item{object}{the deepspat_trivar_GP object}

\item{newdata}{data frame containing the prediction locations}

\item{...}{currently unused}
}
\value{
\code{predict.deepspat_trivar_GP} returns a list with the following item
\describe{
 \item{"df_pred"}{Data frame containing the predictions/prediction intervals at the prediction locations}
 }
}
\description{
Prediction function for the fitted deepspat_trivar_GP object
}
\examples{
df <- data.frame(s1 = rnorm(100), s2 = rnorm(100),
                 z1 = rnorm(100), z2 = rnorm(100), z3 = rnorm(100))
dfnew <- data.frame(s1 = rnorm(20), s2 = rnorm(20))
layers <- c(AWU(r = 50, dim = 1L, grad = 200, lims = c(-0.5, 0.5)))
\dontrun{
\dontrun{d <- deepspat_trivar_GP(f = z1 + z2 + z3 ~ s1 + s2 - 1,
                                 data = df, g = ~ 1,
                                 layers = layers, method = "REML",
                                 family = "matern_nonstat_symm",
                                 nsteps = 100L)}
  pred <- predict.deepspat_trivar_GP(d, dfnew)
}
}
